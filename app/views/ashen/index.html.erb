<!DOCTYPE html>
<html class="no-js" ng-app="ashen">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
        <title>Ashen</title>
        <meta name="description" content="AngularJS Powered Javascript RPG">
        <meta name="viewport" content="width=device-width">
        <%= stylesheet_link_tag "ashen/manifest", :media => "all" %>
        <%= javascript_include_tag "ashen/manifest" %>
    </head>
    <body>
        <div id="container" ng-cloak class="ng-cloak">
            <!-- Player Battle Frame Cell -->
            <div id="player" ng-controller="PlayerController">
                <div class="name-row">
                    <span class="level">LV<em>{{player.experience.level}}</em></span>
                    <span class="name">{{player.name}}</span>
                </div>
                <div class="title-row">
                    <bar type="xp" current="player.experience.xp" max="player.experience.xpm"></bar>
                    <span class="title">The Beginner</span>
                </div>
                <bar type="hp" current="player.stats.hp" max="player.stats.hpm"></bar>
                <bar type="mp" current="player.stats.mp" max="player.stats.mpm"></bar>
            </div>
            <!-- Pet Battle Frame Cell -->
            <div id="pet" ng-controller="PlayerController">
                <div class="name-row">
                    <span class="level">LV<em>15</em></span>
                    <span class="name">Ghost</span>
                </div>
                <div class="title-row">
                    <bar type="xp" current="1" max="1"></bar>
                    <span class="title">Direwolf</span>
                </div>
                <bar type="hp" current="player.stats.hp" max="player.stats.hpm"></bar>
                <bar type="ap" current="player.stats.mp" max="player.stats.mpm"></bar>
            </div>
            <!-- Enemy Battle Frame Cell -->
            <div id="enemy" ng-controller="PlayerController">
                <div class="name-row">
                    <span class="level">LV<em>9</em></span>
                    <span class="name">Goblin</span>
                </div>
                <div class="title-row">
                    <span class="affixes">Molten, Fast</span>
                    <span class="title">Elite</span>
                </div>
                <bar type="hp" current="player.stats.hp" max="player.stats.hpm"></bar>
            </div>
            <!-- Stats Window -->
            <div id="stats" ng-controller="PlayerController">
                <div class="stat"><span class="name">STRENGTH</span><span class="value">{{player.stats.strength}}</span></div>
                <div class="stat"><span class="name">DEFENSE</span><span class="value">{{player.stats.defense}}</span></div>
                <div class="stat"><span class="name">AGILITY</span><span class="value">{{player.stats.agility}}</span></div>
                <div class="stat"><span class="name">INTELLECT</span><span class="value">{{player.stats.intellect}}</span></div>
                <div class="stat"><span class="name">WISDOM</span><span class="value">{{player.stats.wisdom}}</span></div>
            </div>
            <div id="combat" ng-controller="GameController">
                <log id="log">
                    <div ng-repeat="message in log.messages">{{message}}</div>
                </log>
            </div>
            <div id="inventory"></div>
            <div id="tabs"></div>
        </div>
        
        <!--
        <div ng-controller="PlayerController">
            Name: {{player.name}}<br>
            Level: {{player.experience.level}} ({{player.experience.xp}} / {{player.experience.xpm}})<br>
            Strength: {{player.stats.strength}}<br>
            Defense: {{player.stats.defense}}<br>
            Agility: {{player.stats.agility}}<br>
            Intellect: {{player.stats.intellect}}<br>
            Wisdom: {{player.stats.wisdom}}<br><br>
        </div>
        
        <div ng-controller="GameController">
            <div class="experience-button" ng-click="gainExperience(100)">CLICK TO GAIN EXPERIENCE</div>
        </div>
        -->
    </body>
</html>