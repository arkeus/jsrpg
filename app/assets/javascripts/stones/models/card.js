var Card = function(data) {
    data = data || {};
    
    var effects = data['effects'];
    var name = data['name'];
    var description = data['description'];
    var cost = data['cost'] || 0;
};
